let (:export, ...) = //{./spanner};

let sysl = //{//pkg/arrai/sysl.arrai};
let sample = sysl.normalize(sysl.load('../test-data/spanner/sample.sysl.pb'));

(
    export: (
        sample: //seq.has_prefix("CREATE TABLE Types", export(sample))
    )
)
