info:
  description: |
    No description.
  title: Simple
  version: 0.0.0
paths:
  /test:
    get:
      consumes:
      - application/json
      description: No description.
      parameters:
      - format: string
        in: query
        name: key
        type: string
      - format: string
        in: query
        name: min_date
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: No description.
swagger: "2.0"
