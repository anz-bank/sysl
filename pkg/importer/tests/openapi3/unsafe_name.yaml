openapi: "3.0"
info:
  title: Unsafe
paths:
  /:
    get:
      responses:
        200:
          description: "200 OK"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/200Response"
components:
  schemas:
    200Response:
      type: object
      properties:
        name:
          type: string
