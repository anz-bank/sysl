openapi: "3.0"
info:
  title: Options
paths:
  /:
    options:
      summary: test options
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: string
    head:
      summary: test head
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: string
    trace:
      summary: test trace
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: string
