swagger: "2.0"
info:
  title: Unsafe Type Names
  version: "1"
paths:
  /start_with_number:
    get:
      responses:
        200:
          description: 200 OK
          schema:
            $ref: '#/definitions/200unsafe'
      description: starts with a number
definitions:
  200unsafe:
    type: string
