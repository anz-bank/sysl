let import = //{/pkg/importer/avro/transformer};

let schema = //[//encoding.bytes]{./datatypes.avsc};

(
    import: import(schema, "TestApp", "com.example.package") = $`${//[//encoding.bytes]{./datatypes.sysl}}` ++ '\n'
)
