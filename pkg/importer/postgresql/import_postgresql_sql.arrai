# Generates a Sysl spec equivalent to the model produced by applying the sequence of statements in
# the file importSql.
let [_, importSql, appName, syslPackage] = //os.args;

//{./import}.import([importSql], appName, syslPackage)
