let [_, importSql] = //os.args;
let spanner = //{./spanner.arrai};
let sysl = //{./sysl.arrai};
let stmts = spanner.parseSchema([importSql]);
let model = spanner.applyStmt(stmts, spanner.emptyModel);
$`${sysl.transformModel(model)}`
