swagger: "2.0"
info:
  title: Simple
paths:
  /test:
    get:
      responses:
        200:
          description: 200 OK
      parameters:
        - name: key
          type: string
          required: false
          in: query
        - name: min_date
          type: string
          required: true
          in: query
          format: date
