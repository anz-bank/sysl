let (:import, ...) = //{./proto};

let config = {
    'proto-path': (type: 'string', repeated: true, alias: 'dir', usage: 'The path at which to search for imports.'),
};

let args = //flag.parser(config).parse(//os.args);
let baseFile = args.args(1);
let protoPaths = args.flags('proto-path');

import(baseFile, protoPaths)