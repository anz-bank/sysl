# A demo of the `normalize` feature that transform the "all" Sysl proto
# model into a relational model.

let sysl = //{./sysl};
let model = sysl.load('out/all.pb');

let out = sysl.newNormalize(model);

//fmt.pretty(out)
