let (:concat, ...) = //{./concat};

(
    util: concat('util.arrai') = $`
### ------------------------------------------------------------------------ ###
###  util.arrai                                                              ###
### ------------------------------------------------------------------------ ###

${//os.file('util.arrai')}`
)
