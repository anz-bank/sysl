Server [~rest]:
    /first:
        GET:
            ...
        /{id<:int}:
            GET ?depth=int&limit=int?&offset=int?:
                ...
            /second:
                PUT:
                    return 200 ok
                /third:
                    POST:
                        ...
            /another-second:
                POST:
                    ...
    /first-level/{id<:int}/second/third/{subid<:id}:
        GET ?q={queryTerms}&limit={pageSize}&offset={startingRecord} [id='id01']:
            ...

BrowserApp [~ajax]:
    On Click:
        | Comment On click, initiate a server call
        Server <- GET /first/{id}
