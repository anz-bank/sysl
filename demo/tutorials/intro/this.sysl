MobileApp:
  Login:
    Server <- Login
# Server: defines the server application
Server:
  #  Login takes a Login datatype as query type
  Login(data <: LoginData):
    build query
    DB <- Query
    check result
    return Server.LoginResponse

  !type LoginData:
    username <: string
    password <: string

  !type LoginResponse:
    message <: string

DB:
  Query:
    lookup data
    return data
  Save:
    ...