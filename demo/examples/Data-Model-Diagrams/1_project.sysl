# In this example will use a simple system and start using the sysl command to generate diagrams.


Project:
    _:
        App

App:
    !type Order:
        order_id <: int

    !type Customer:
        customer_id <: int
        orders <: set of Order
