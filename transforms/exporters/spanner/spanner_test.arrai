let (:export, ...) = //{./spanner};

let (:loadBytes, :newNormalize, ...) = //{/pkg/arrai/sysl};
let sample = newNormalize(loadBytes(//[//encoding.bytes]{/pkg/exporter/test-data/spanner/sample.sysl.pb}));

(
    export: (
        sample: //seq.has_prefix("CREATE TABLE Types", export(sample))
    )
)
