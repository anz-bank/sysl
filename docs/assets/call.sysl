MobileApp:
  Login:
    Server <- Login

Server:
  Login(data <: LoginData):
    build query
    DB <- Query
    check result
    return success or failure

  !type LoginData:
    username <: string
    password <: string

DB:
  Query:
    ...
  Save:
    ...

Project [seqtitle="Diagram"]:
  Seq:
    MobileApp <- Login
